# SMTP Configuration (optional — without it, form submissions are accepted but not emailed)

# Resend (recommended — free tier: 100 emails/day)
#   1. Sign up at https://resend.com
#   2. Get an API key
#   3. Use onboarding@resend.dev as FROM_EMAIL (or verify your domain)

# Gmail (requires 2-Step Verification + App Password)
#   1. Enable 2FA at https://myaccount.google.com/security
#   2. Generate App Password at https://myaccount.google.com/apppasswords
#   3. Use that 16-char code as SMTP_PASS

SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=resend
SMTP_PASS=re_...
FROM_EMAIL=onboarding@resend.dev
CONTACT_TO=your-email@example.com

# The site's public origin. It is the canonical/hreflang base in the page head,
# the domain in sitemap.xml and robots.txt, the origin the contact API accepts
# posts from, and the Cypress baseUrl. In production set it to the real domain —
# a local value here ships localhost URLs to search engines.
# Locally: http://localhost:3000
SITE_URL=https://mvahdati.ir
